/tr/ / 301
/episodes/* https://media.eigenradar.com/episodes/:splat 301
/assets/briefs/* https://media.eigenradar.com/assets/briefs/:splat 301
/assets/videos/* https://media.eigenradar.com/assets/videos/:splat 301

# Legacy edition-suffix column URLs consolidated to the writer's daily page (2026-08-29).
# Collapsed to placeholder rules: Cloudflare Workers static assets cap _redirects at 100
# dynamic rules and counted the previous 1,078 exact-path rules against that cap, so the
# per-URL list was undeployable (deploy error 100324). Editions never exceed n=4, and a
# Brief id must contain a letter, so the literal /2/ /3/ /4/ tails cannot collide with any
# live route. A future distinctColumn /2/ page must add a pass-through above these rules.
/tr/:topic/:writer/:date/2/ /tr/:topic/:writer/:date/ 301
/tr/:topic/:writer/:date/3/ /tr/:topic/:writer/:date/ 301
/tr/:topic/:writer/:date/4/ /tr/:topic/:writer/:date/ 301
/en/:topic/:writer/:date/2/ /en/:topic/:writer/:date/ 301
/en/:topic/:writer/:date/3/ /en/:topic/:writer/:date/ 301
/en/:topic/:writer/:date/4/ /en/:topic/:writer/:date/ 301
