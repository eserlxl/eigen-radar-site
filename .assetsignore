# The Cloudflare Workers build deploys this repository root as static assets.
# Repository and local-tool internals are never public site files.
**/.git
.git/**
**/.git/**
**/.remember
.remember/**
**/.remember/**
**/.DS_Store

# Build-only npm metadata pins Wrangler and must not become public site content.
package.json
package-lock.json
